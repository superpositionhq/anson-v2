# {{WORKSPACE_NAME}} env vars
# Populated by anson-v2 install. Source with: `source .env`
# chmod 600 — never check into git.

# ---- Gmail / Google ----
{{#GMAIL_ACCOUNTS}}
# {{.}}
{{/GMAIL_ACCOUNTS}}
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=

# ---- Slack ----
SLACK_WORKSPACE={{SLACK_WORKSPACE_URL}}
# User session tokens (DevTools extraction; expire ~30d, refresh as needed)
SLACK_XOXC_TOKEN=
SLACK_XOXD_TOKEN=
{{#IF_SLACK_BRIDGE}}
# Bot tokens for the two-way bridge
SLACK_BOT_TOKEN=
SLACK_APP_TOKEN=
{{/IF_SLACK_BRIDGE}}

# ---- Notion ----
NOTION_API_KEY=

# ---- iMessage ----
{{#IF_IMESSAGE}}
# macOS — requires Full Disk Access. Use a literal absolute path
# (dotenv does not shell-expand $HOME / ~).
IMESSAGE_DB={{HOME_ABSOLUTE}}/Library/Messages/chat.db
{{/IF_IMESSAGE}}

# ---- Custom (added by Stage E workflows) ----
